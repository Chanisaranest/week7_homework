pipeline {
    agent any
    
    stages {
        stage('Example') {
            steps {
                echo("running ...")
                sh ("python ./myscript.py " + params.NAME + " --age " + params.Ages)
            }
        }
    }
}
